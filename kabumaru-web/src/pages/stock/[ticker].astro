---
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { getCollection } from 'astro:content';
import { SITE_TITLE } from '../../consts';

export async function getStaticPaths() {
  return [
    { params: { ticker: '5803' } },
    { params: { ticker: '9432' } },
    { params: { ticker: '7735' } },
    { params: { ticker: '4004' } },
    { params: { ticker: '8521' } },
  ];
}

const { ticker } = Astro.params;

// Static Mock database for stocks matching the JP theme
const stocksDb = {
  "5803": {
    name: "フジクラ",
    fullname: "株式会社フジクラ",
    exchange: "東証プライム",
    tags: ["電線", "光ファイバ", "データセンター"],
    price: "2,845.5",
    change: "+124.5",
    changePercent: "+4.58%",
    isPositive: true,
    time: "終値 06/12 15:00 JST",
    aiScore: "88.5",
    aiVerdict: "非常に強い買い",
    scoreMeta: {
      growth: 92,
      profitability: 85,
      momentum: 94,
      value: 40,
      health: 80
    },
    founded: "1885年",
    hq: "東京都江東区木場",
    ceo: "岡田 直樹",
    employees: "54,200人",
    site: "fujikura.co.jp",
    overview: "株式会社フジクラは、世界大手の電線・光ファイバメーカー。生成AIデータセンター向けに、独自の超多心光ファイバケーブルとコネクタが圧倒的な世界シェアを誇り、急成長中。",
    metrics: {
      mcap: "8,250億円",
      per: "15.4x",
      pbr: "2.1x",
      roe: "14.8%",
      revGrowth: "+24.5%",
      netMargin: "9.2%"
    },
    sparkline: "M0 25 L10 23 L20 28 L30 18 L40 20 L50 10 L60 14 L70 5 L80 12 L90 2 L100 4",
    videos: [
      {
        title: "フジクラ（5803）急騰の真相！AIデータセンター特需で大化けする光コネクタの将来性",
        duration: "15:20",
        views: "5.4万回視聴",
        date: "3日前"
      }
    ]
  },
  "9432": {
    name: "日本電信電話",
    fullname: "日本電信電話株式会社 (NTT)",
    exchange: "東証プライム",
    tags: ["通信", "ディフェンシブ", "IOWN"],
    price: "148.2",
    change: "-1.5",
    changePercent: "-1.00%",
    isPositive: false,
    time: "終値 06/12 15:00 JST",
    aiScore: "81.2",
    aiVerdict: "買い",
    scoreMeta: {
      growth: 70,
      profitability: 82,
      momentum: 60,
      value: 65,
      health: 95
    },
    founded: "1985年 (民営化)",
    hq: "東京都千代田区大手町",
    ceo: "島田 明",
    employees: "330,000人",
    site: "group.ntt",
    overview: "日本電信電話株式会社 (NTT) は、国内最大の総合通信グループ。新NISAの普及とともに、個人株主が急増中。次世代の光電融合ネットワーク「IOWN構想」を推進している。",
    metrics: {
      mcap: "13.2兆円",
      per: "11.5x",
      pbr: "1.2x",
      roe: "10.4%",
      revGrowth: "+3.2%",
      netMargin: "11.1%"
    },
    sparkline: "M0 10 L20 12 L40 8 L60 14 L80 20 L100 25",
    videos: [
      {
        title: "NTT（9432）は今から買っていい？新NISAで大人気も株価が上がらない理由と将来性",
        duration: "12:15",
        views: "11.2万回視聴",
        date: "1週間前"
      }
    ]
  },
  "7735": {
    name: "SCREEN",
    fullname: "SCREENホールディングス",
    exchange: "東証プライム",
    tags: ["半導体", "製造装置", "洗浄"],
    price: "15,820.0",
    change: "+430.0",
    changePercent: "+2.79%",
    isPositive: true,
    time: "終値 06/12 15:00 JST",
    aiScore: "86.4",
    aiVerdict: "買い",
    scoreMeta: {
      growth: 88,
      profitability: 82,
      momentum: 74,
      value: 35,
      health: 85
    },
    founded: "1943年",
    hq: "京都府京都市上京区",
    ceo: "廣江 敏治",
    employees: "6,500人",
    site: "screen.co.jp",
    overview: "SCREENホールディングスは、半導体製造用のウェーハ洗浄装置で世界シェア約7割を誇る圧倒的なトップ企業。半導体の微細化・3D化プロセスにおいて不可欠な存在。",
    metrics: {
      mcap: "7,800億円",
      per: "18.2x",
      pbr: "3.4x",
      roe: "20.1%",
      revGrowth: "+18.9%",
      netMargin: "13.5%"
    },
    sparkline: "M0 25 L20 22 L40 18 L60 12 L80 15 L100 8",
    videos: [
      {
        title: "SCREEN（7735）洗浄装置シェア7割の超強み！株価調整は絶好の買い場になるか",
        duration: "14:10",
        views: "4.8万回視聴",
        date: "2週間前"
      }
    ]
  },
  "4004": {
    name: "レゾナック",
    fullname: "株式会社レゾナック・ホールディングス",
    exchange: "東証プライム",
    tags: ["半導体材料", "化学", "V字回復"],
    price: "3,820.0",
    change: "+150.0",
    changePercent: "+4.09%",
    isPositive: true,
    time: "終値 06/12 15:00 JST",
    aiScore: "91.2",
    aiVerdict: "非常に強い買い",
    scoreMeta: {
      growth: 95,
      profitability: 70,
      momentum: 90,
      value: 48,
      health: 72
    },
    founded: "1939年",
    hq: "東京都港区芝大門",
    ceo: "髙橋 秀仁",
    employees: "26,000人",
    site: "resonac.com",
    overview: "株式会社レゾナック・ホールディングス（旧昭和電工）は、半導体後工程（パッケージング）用の周辺材料において世界首位。AI半導体（2.5D/3D実装）市場の拡大で材料特需が発生中。",
    metrics: {
      mcap: "6,950億円",
      per: "14.2x",
      pbr: "1.8x",
      roe: "11.2%",
      revGrowth: "+28.2%",
      netMargin: "6.8%"
    },
    sparkline: "M0 28 L20 26 L40 18 L60 12 L80 10 L100 5",
    videos: [
      {
        title: "レゾナック（4004）AI半導体後工程で世界最強！素材株の中で今一番仕込みたい理由",
        duration: "16:40",
        views: "7.8万回視聴",
        date: "5日前"
      }
    ]
  },
  "8521": {
    name: "ソニーFG",
    fullname: "ソニーフィナンシャルグループ",
    exchange: "東証プライム",
    tags: ["金融", "生保", "高配当"],
    price: "3,650.0",
    change: "-25.0",
    changePercent: "-0.68%",
    isPositive: false,
    time: "終値 06/12 15:00 JST",
    aiScore: "78.9",
    aiVerdict: "中立",
    scoreMeta: {
      growth: 68,
      profitability: 80,
      momentum: 65,
      value: 55,
      health: 90
    },
    founded: "2004年",
    hq: "東京都千代田区大手町",
    ceo: "岡 昌志",
    employees: "11,000人",
    site: "sonyfi.co.jp",
    overview: "ソニーフィナンシャルグループは、ソニーグループ傘下の金融持株会社。ソニー生命、ソニー損保、ソニー銀行などを傘下に収め、利上げ局面における長期金利上昇で業績拡大期待が強い。",
    metrics: {
      mcap: "1.5兆円",
      per: "12.8x",
      pbr: "1.1x",
      roe: "8.9%",
      revGrowth: "+6.8%",
      netMargin: "7.9%"
    },
    sparkline: "M0 12 L20 15 L40 10 L60 18 L80 20 L100 24",
    videos: [
      {
        title: "ソニーFG（8521）金利上昇で金融株は買いなのか？生命保険・銀行のビジネスモデルをガチ解説",
        duration: "13:40",
        views: "3.5万回視聴",
        date: "3週間前"
      }
    ]
  }
};

const stock = stocksDb[ticker as keyof typeof stocksDb] || stocksDb["5803"];

// Fetch blog articles matching the ticker or name
const allPosts = await getCollection('blog');
const relatedPosts = allPosts.filter(post => 
  post.data.title.toLowerCase().includes(stock.name.toLowerCase()) ||
  post.body?.toLowerCase().includes(stock.name.toLowerCase())
).slice(0, 3);

// Fallback logic if no posts found: show general latest posts
const displayedPosts = relatedPosts.length > 0 ? relatedPosts : allPosts.slice(0, 3);

// Calculations for custom SVG pentagon radar chart
// Center = (50, 50), Max Radius = 40
// Angles (rad): 90deg, 162deg, 234deg, 306deg, 18deg
const computeRadarPoints = (score: typeof stock.scoreMeta) => {
  const rMax = 38;
  const cx = 50;
  const cy = 50;
  
  // Ratios (0.0 to 1.0)
  const g = score.growth / 100;
  const p = score.profitability / 100;
  const m = score.momentum / 100;
  const v = score.value / 100;
  const h = score.health / 100;
  
  // Point 1: Top (Growth) - 270 deg (in SVG, Y goes down, so Y = 50 - r)
  const x1 = cx;
  const y1 = cy - g * rMax;
  
  // Point 2: Right Top (Profitability) - 342 deg (in Y-down, Y = 50 - r * sin(18deg), X = 50 + r * cos(18deg))
  const x2 = cx + p * rMax * Math.cos(18 * Math.PI / 180);
  const y2 = cy - p * rMax * Math.sin(18 * Math.PI / 180);
  
  // Point 3: Right Bottom (Momentum) - 54 deg (Y = 50 + r * sin(54deg), X = 50 + r * cos(54deg))
  const x3 = cx + m * rMax * Math.cos(54 * Math.PI / 180);
  const y3 = cy + m * rMax * Math.sin(54 * Math.PI / 180);
  
  // Point 4: Left Bottom (Value) - 126 deg (Y = 50 + r * sin(54deg), X = 50 - r * cos(54deg))
  const x4 = cx - v * rMax * Math.cos(54 * Math.PI / 180);
  const y4 = cy + v * rMax * Math.sin(54 * Math.PI / 180);
  
  // Point 5: Left Top (Health) - 198 deg (Y = 50 - r * sin(18deg), X = 50 - r * cos(18deg))
  const x5 = cx - h * rMax * Math.cos(18 * Math.PI / 180);
  const y5 = cy - h * rMax * Math.sin(18 * Math.PI / 180);
  
  return `${x1},${y1} ${x2},${y2} ${x3},${y3} ${x4},${y4} ${x5},${y5}`;
};

const radarPoints = computeRadarPoints(stock.scoreMeta);
---

<!doctype html>
<html lang="ja">
	<head>
		<BaseHead title={`${stock.name} (${ticker}) - ${SITE_TITLE}`} description={stock.overview} />
	</head>
	<body>
		<Header />
		
		<main class="stock-main fade-in">
			<!-- Breadcrumb -->
			<div class="breadcrumb">
				<a href="/">トップ</a> &gt; <a href="/ranking/ai">銘柄一覧</a> &gt; <span class="current">{stock.name}</span>
			</div>

			<!-- Stock Main Header Panel -->
			<div class="glass-card stock-header-panel">
				<div class="stock-title-section">
					<div class="stock-brand">
						<div class="brand-logo-circle">
							<span class="logo-char">{stock.name.charAt(0)}</span>
						</div>
						<div class="brand-text">
							<h1 class="brand-name">{stock.name}</h1>
							<span class="brand-ticker-exchange">{ticker} ｜ {stock.exchange}</span>
						</div>
					</div>
					<div class="stock-tags">
						{stock.tags.map(tag => <span class="stock-tag">{tag}</span>)}
					</div>
					<div class="stock-actions">
						<button class="btn btn-outline action-btn"><span class="star-icon">☆</span> ウォッチリストに追加</button>
						<button class="btn btn-outline action-btn-icon" aria-label="共有">
							<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="16" height="16">
								<circle cx="18" cy="5" r="3"></circle>
								<circle cx="6" cy="12" r="3"></circle>
								<circle cx="18" cy="19" r="3"></circle>
								<line x1="8.59" y1="13.51" x2="15.42" y2="17.49"></line>
								<line x1="15.41" y1="6.51" x2="8.59" y2="10.49"></line>
							</svg>
						</button>
					</div>
				</div>

				<div class="stock-price-section">
					<div class="price-info">
						<span class="price-val">¥{stock.price}</span>
						<span class={`price-change ${stock.isPositive ? 'positive' : 'negative'}`}>
							{stock.change} ({stock.changePercent})
						</span>
					</div>
					<div class="price-time">{stock.time}</div>
					<div class="sparkline-wrapper">
						<svg class="header-sparkline" viewBox="0 0 100 30">
							<path d={stock.sparkline} fill="none" stroke={stock.isPositive ? 'var(--positive)' : 'var(--negative)'} stroke-width="2.5" stroke-linecap="round"/>
						</svg>
					</div>
				</div>
			</div>

			<!-- Tabs Navigation -->
			<div class="stock-tabs-container">
				<div class="stock-tabs">
					<a href="#" class="tab-link active">概要</a>
					<a href="#articles" class="tab-link">分析記事</a>
					<a href="#videos" class="tab-link">動画</a>
					<a href="#earnings" class="tab-link">決算</a>
					<a href="#financials" class="tab-link">財務指標</a>
					<a href="#news" class="tab-link">ニュース</a>
					<a href="#chart" class="tab-link">チャート</a>
					<a href="#related" class="tab-link">関連銘柄</a>
				</div>
			</div>

			<!-- Three-Column Details Grid -->
			<div class="details-grid">
				<!-- Column 1: AI Score & Radar Chart -->
				<div class="glass-card details-column-card">
					<h3 class="column-card-title">AIスコア</h3>
					<div class="ai-score-summary">
						<span class="ai-score-number">{stock.aiScore} <span class="score-base">/ 100</span></span>
						<span class="ai-score-label badge badge-positive">{stock.aiVerdict}</span>
					</div>
					
					<!-- Radar Chart SVG Container -->
					<div class="radar-chart-container">
						<svg class="radar-chart-svg" viewBox="0 0 100 100">
							<!-- Concentric Grid Pentagons -->
							<polygon points="50,15 88,43 74,88 26,88 12,43" fill="none" stroke="rgba(0, 0, 0, 0.05)" stroke-width="1"/>
							<polygon points="50,26.5 75.3,45.3 66,75.3 34,75.3 24.7,45.3" fill="none" stroke="rgba(0, 0, 0, 0.05)" stroke-width="1"/>
							<polygon points="50,38 62.7,47.7 58,62.7 42,62.7 37.3,47.7" fill="none" stroke="rgba(0, 0, 0, 0.05)" stroke-width="1"/>
							
							<!-- Grid Axis Lines -->
							<line x1="50" y1="50" x2="50" y2="15" stroke="rgba(0, 0, 0, 0.05)" stroke-width="1"/>
							<line x1="50" y1="50" x2="88" y2="43" stroke="rgba(0, 0, 0, 0.05)" stroke-width="1"/>
							<line x1="50" y1="50" x2="74" y2="88" stroke="rgba(0, 0, 0, 0.05)" stroke-width="1"/>
							<line x1="50" y1="50" x2="26" y2="88" stroke="rgba(0, 0, 0, 0.05)" stroke-width="1"/>
							<line x1="50" y1="50" x2="12" y2="43" stroke="rgba(0, 0, 0, 0.05)" stroke-width="1"/>

							<!-- Actual Scaled Value Pentagon -->
							<polygon points={radarPoints} fill="rgba(255, 107, 0, 0.2)" stroke="var(--accent)" stroke-width="2"/>
							
							<!-- Labels on Axis -->
							<text x="50" y="10" text-anchor="middle" font-size="6" fill="var(--text-secondary)" font-weight="700">成長性</text>
							<text x="91" y="44" text-anchor="start" font-size="6" fill="var(--text-secondary)" font-weight="700">収益性</text>
							<text x="76" y="93" text-anchor="start" font-size="6" fill="var(--text-secondary)" font-weight="700">モメンタム</text>
							<text x="24" y="93" text-anchor="end" font-size="6" fill="var(--text-secondary)" font-weight="700">割安性</text>
							<text x="9" y="44" text-anchor="end" font-size="6" fill="var(--text-secondary)" font-weight="700">財務健全</text>
						</svg>
					</div>
					<div class="score-action-box">
						<a href="#scores" class="score-detail-link">スコアの詳細を見る &gt;</a>
					</div>
				</div>

				<!-- Column 2: 企業概要 (Company Overview) -->
				<div class="glass-card details-column-card">
					<h3 class="column-card-title">企業概要</h3>
					<p class="company-desc">{stock.overview}</p>
					
					<div class="company-info-table">
						<div class="info-row">
							<span class="info-label">設立</span>
							<span class="info-value">{stock.founded}</span>
						</div>
						<div class="info-row">
							<span class="info-label">本社</span>
							<span class="info-value">{stock.hq}</span>
						</div>
						<div class="info-row">
							<span class="info-label">代表者 (CEO)</span>
							<span class="info-value">{stock.ceo}</span>
						</div>
						<div class="info-row">
							<span class="info-label">連結従業員数</span>
							<span class="info-value">{stock.employees}</span>
						</div>
						<div class="info-row">
							<span class="info-label">公式サイト</span>
							<span class="info-value"><a href={`https://${stock.site}`} target="_blank" class="company-link">{stock.site} ↗</a></span>
						</div>
					</div>
				</div>

				<!-- Column 3: 主要指標 (Key Metrics) -->
				<div class="glass-card details-column-card">
					<h3 class="column-card-title">主要指標</h3>
					
					<div class="metrics-list">
						<div class="metric-row">
							<span class="metric-label">時価総額</span>
							<span class="metric-value">{stock.metrics.mcap}</span>
						</div>
						<div class="metric-row">
							<span class="metric-label">PER (予想)</span>
							<span class="metric-value">{stock.metrics.per}</span>
						</div>
						<div class="metric-row">
							<span class="metric-label">PBR</span>
							<span class="metric-value">{stock.metrics.pbr}</span>
						</div>
						<div class="metric-row">
							<span class="metric-label">ROE</span>
							<span class="metric-value">{stock.metrics.roe}</span>
						</div>
						<div class="metric-row">
							<span class="metric-label">売上高成長率</span>
							<span class="metric-value positive">{stock.metrics.revGrowth}</span>
						</div>
						<div class="metric-row">
							<span class="metric-label">純利益率</span>
							<span class="metric-value positive">{stock.metrics.netMargin}</span>
						</div>
					</div>
					<div class="metrics-action-box">
						<a href="#metrics" class="metrics-detail-link">財務指標をもっと見る &gt;</a>
					</div>
				</div>
			</div>

			<!-- Related Content (Videos & Articles) -->
			<div class="related-content-section" id="videos">
				<div class="section-header">
					<h2 class="section-title">関連動画</h2>
					<a href="/#videos" class="view-all-link">すべて見る &gt;</a>
				</div>
				<div class="videos-grid">
					{stock.videos.map(video => (
						<div class="glass-card video-card">
							<div class="video-thumbnail-placeholder">
								<div class="video-play-btn">
									<svg viewBox="0 0 24 24" fill="currentColor" width="24" height="24">
										<path d="M8 5v14l11-7z"/>
									</svg>
								</div>
								<span class="video-duration">{video.duration}</span>
							</div>
							<div class="video-info">
								<h4 class="video-title">{video.title}</h4>
								<div class="video-meta">
									<span class="video-author">かぶまる</span>
									<span class="video-views-date">{video.views} ｜ {video.date}</span>
								</div>
							</div>
						</div>
					))}
				</div>
			</div>

			<div class="related-content-section" id="articles">
				<div class="section-header">
					<h2 class="section-title">関連記事</h2>
					<a href="/" class="view-all-link">すべて見る &gt;</a>
				</div>
				<div class="articles-grid">
					{displayedPosts.map(post => (
						<article class="glass-card article-card-small">
							<a href={`/blog/${post.id}/`} class="article-link-small">
								<div class="article-meta-small">
									<span class="badge badge-positive">銘柄分析</span>
									<time class="article-date-small">
										{post.data.pubDate.toLocaleDateString('ja-JP', {
											year: 'numeric',
											month: '2-digit',
											day: '2-digit',
										})}
									</time>
								</div>
								<h4 class="article-title-small">{post.data.title}</h4>
								<p class="article-desc-small">{post.data.description}</p>
								<span class="article-read-link-small">記事を読む &rarr;</span>
							</a>
						</article>
					))}
				</div>
			</div>
		</main>

		<Footer />
	</body>
</html>

<style>
	.stock-main {
		max-width: 1200px;
		margin: 0 auto;
		padding: 2rem 1.5rem;
	}
	.breadcrumb {
		font-size: 0.85rem;
		color: var(--text-secondary);
		margin-bottom: 2rem;
	}
	.breadcrumb a {
		color: var(--text-secondary);
	}
	.breadcrumb a:hover {
		color: var(--accent);
	}
	.breadcrumb .current {
		color: var(--text-muted);
	}

	/* Stock Header Panel Styles */
	.stock-header-panel {
		padding: 2.2rem;
		display: flex;
		justify-content: space-between;
		align-items: center;
		margin-bottom: 2.5rem;
		gap: 2rem;
	}
	.stock-title-section {
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}
	.stock-brand {
		display: flex;
		align-items: center;
		gap: 1.25rem;
	}
	.brand-logo-circle {
		width: 52px;
		height: 52px;
		border-radius: 12px;
		background: linear-gradient(135deg, var(--accent) 0%, var(--accent-light) 100%);
		display: flex;
		align-items: center;
		justify-content: center;
		box-shadow: 0 4px 12px var(--accent-glow);
	}
	.logo-char {
		font-family: var(--font-display);
		font-size: 1.8rem;
		font-weight: 800;
		color: #ffffff;
	}
	.brand-name {
		font-size: 2rem;
		font-weight: 800;
		color: var(--text-primary);
		line-height: 1.1;
	}
	.brand-ticker-exchange {
		font-size: 0.9rem;
		color: var(--text-secondary);
	}
	.stock-tags {
		display: flex;
		gap: 0.5rem;
	}
	.stock-tag {
		font-size: 0.75rem;
		padding: 0.2rem 0.6rem;
		border-radius: 4px;
		background: rgba(0, 0, 0, 0.03);
		border: 1px solid rgba(0, 0, 0, 0.05);
		color: var(--text-secondary);
	}
	.stock-actions {
		display: flex;
		gap: 0.6rem;
		margin-top: 0.5rem;
	}
	.action-btn {
		font-size: 0.8rem;
		padding: 0.5rem 1rem;
	}
	.star-icon {
		color: #ffd700;
		margin-right: 0.25rem;
	}
	.action-btn-icon {
		padding: 0.5rem;
		display: flex;
		align-items: center;
		justify-content: center;
		border-radius: 8px;
		background: transparent;
		border: 1px solid rgba(0, 0, 0, 0.12);
		color: var(--text-secondary);
		cursor: pointer;
		width: 34px;
		height: 34px;
	}
	.action-btn-icon:hover {
		color: var(--accent);
		border-color: var(--accent);
		background: rgba(255, 107, 0, 0.02);
	}

	.stock-price-section {
		display: flex;
		flex-direction: column;
		align-items: flex-end;
		text-align: right;
		gap: 0.25rem;
	}
	.price-info {
		display: flex;
		align-items: baseline;
		gap: 0.75rem;
	}
	.price-val {
		font-family: var(--font-display);
		font-size: 2.5rem;
		font-weight: 800;
		color: var(--text-primary);
		line-height: 1;
	}
	.price-change {
		font-family: var(--font-display);
		font-size: 1.15rem;
		font-weight: 700;
	}
	.price-change.positive { color: var(--positive); }
	.price-change.negative { color: var(--negative); }
	.price-time {
		font-size: 0.8rem;
		color: var(--text-muted);
	}
	.sparkline-wrapper {
		margin-top: 0.5rem;
	}
	.header-sparkline {
		width: 140px;
		height: 40px;
		overflow: visible;
	}

	/* Tabs Styles */
	.stock-tabs-container {
		border-bottom: 1px solid rgba(0, 0, 0, 0.06);
		margin-bottom: 2.5rem;
	}
	.stock-tabs {
		display: flex;
		gap: 2.2rem;
		overflow-x: auto;
		padding-bottom: 0.1rem;
	}
	.tab-link {
		font-size: 0.95rem;
		font-weight: 600;
		color: var(--text-secondary);
		padding: 0.6rem 0.2rem 1rem 0.2rem;
		position: relative;
		white-space: nowrap;
	}
	.tab-link:hover,
	.tab-link.active {
		color: var(--accent);
	}
	.tab-link.active::after {
		content: '';
		position: absolute;
		bottom: -1px;
		left: 0;
		width: 100%;
		height: 2.5px;
		background: var(--accent);
		box-shadow: 0 0 6px var(--accent-glow);
	}

	/* Grid Layout */
	.details-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 1.5rem;
		margin-bottom: 4rem;
	}
	.details-column-card {
		padding: 2rem;
		display: flex;
		flex-direction: column;
		height: 100%;
	}
	.column-card-title {
		font-size: 1.15rem;
		font-weight: 700;
		margin-bottom: 1.5rem;
		position: relative;
		padding-left: 0.7rem;
	}
	.column-card-title::before {
		content: '';
		position: absolute;
		left: 0;
		top: 20%;
		height: 60%;
		width: 3.5px;
		background: var(--accent);
		border-radius: 1px;
	}

	/* AI Score Styles */
	.ai-score-summary {
		display: flex;
		align-items: center;
		justify-content: space-between;
		margin-bottom: 1.5rem;
	}
	.ai-score-number {
		font-family: var(--font-display);
		font-size: 2.2rem;
		font-weight: 800;
		color: var(--text-primary);
	}
	.score-base {
		font-size: 0.95rem;
		color: var(--text-secondary);
		font-weight: 500;
	}
	.radar-chart-container {
		display: flex;
		justify-content: center;
		align-items: center;
		margin: 1.5rem 0;
		flex-grow: 1;
	}
	.radar-chart-svg {
		width: 100%;
		max-width: 200px;
		height: auto;
		overflow: visible;
	}
	.score-action-box {
		margin-top: 1.5rem;
		text-align: center;
	}
	.score-detail-link {
		font-size: 0.85rem;
		font-weight: 600;
		color: var(--text-secondary);
	}
	.score-detail-link:hover {
		color: var(--accent);
	}

	/* Company Info Styles */
	.company-desc {
		font-size: 0.9rem;
		color: var(--text-secondary);
		line-height: 1.6;
		margin-bottom: 1.5rem;
	}
	.company-info-table {
		display: flex;
		flex-direction: column;
		gap: 0.9rem;
		margin-top: auto;
	}
	.info-row {
		display: flex;
		justify-content: space-between;
		align-items: center;
		font-size: 0.88rem;
		border-bottom: 1px solid rgba(0, 0, 0, 0.04);
		padding-bottom: 0.5rem;
	}
	.info-label {
		color: var(--text-secondary);
	}
	.info-value {
		color: var(--text-primary);
		font-weight: 600;
	}
	.company-link {
		color: var(--accent);
		font-weight: 600;
	}

	/* Metrics List Styles */
	.metrics-list {
		display: flex;
		flex-direction: column;
		gap: 1.1rem;
		flex-grow: 1;
	}
	.metric-row {
		display: flex;
		justify-content: space-between;
		align-items: center;
		font-size: 0.9rem;
		border-bottom: 1px solid rgba(0, 0, 0, 0.04);
		padding-bottom: 0.6rem;
	}
	.metric-label {
		color: var(--text-secondary);
	}
	.metric-value {
		font-family: var(--font-display);
		color: var(--text-primary);
		font-weight: 700;
	}
	.metric-value.positive { color: var(--positive); }
	.metric-value.negative { color: var(--negative); }
	.metrics-action-box {
		margin-top: 1.5rem;
		text-align: center;
	}
	.metrics-detail-link {
		font-size: 0.85rem;
		font-weight: 600;
		color: var(--text-secondary);
	}
	.metrics-detail-link:hover {
		color: var(--accent);
	}

	/* Related Content Sections */
	.related-content-section {
		margin-bottom: 4rem;
	}
	.section-header {
		display: flex;
		justify-content: space-between;
		align-items: flex-end;
		margin-bottom: 1.5rem;
	}
	.section-title {
		font-size: 1.5rem;
		font-weight: 700;
		position: relative;
		padding-left: 0.8rem;
	}
	.section-title::before {
		content: '';
		position: absolute;
		left: 0;
		top: 15%;
		height: 70%;
		width: 4px;
		background: linear-gradient(to bottom, var(--accent), #ff9500);
		border-radius: 2px;
	}
	.view-all-link {
		font-size: 0.85rem;
		font-weight: 600;
		color: var(--text-secondary);
	}
	.view-all-link:hover {
		color: var(--accent);
	}
	
	/* Videos Grid Styles */
	.videos-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 1.5rem;
	}
	.video-card {
		overflow: hidden;
		display: flex;
		flex-direction: column;
		height: 100%;
	}
	.video-thumbnail-placeholder {
		aspect-ratio: 16/9;
		background: linear-gradient(135deg, #f3f4f6 0%, rgba(255, 107, 0, 0.04) 100%);
		border-bottom: 1px solid rgba(0, 0, 0, 0.05);
		display: flex;
		align-items: center;
		justify-content: center;
		position: relative;
	}
	.video-play-btn {
		width: 48px;
		height: 48px;
		border-radius: 50%;
		background: var(--accent);
		display: flex;
		align-items: center;
		justify-content: center;
		color: #ffffff;
		box-shadow: 0 4px 12px var(--accent-glow);
		transition: all 0.2s ease;
	}
	.video-card:hover .video-play-btn {
		transform: scale(1.1);
		box-shadow: 0 6px 20px rgba(255, 107, 0, 0.3);
	}
	.video-duration {
		position: absolute;
		bottom: 8px;
		right: 8px;
		font-size: 0.75rem;
		background: rgba(0, 0, 0, 0.75);
		padding: 0.1rem 0.4rem;
		border-radius: 4px;
		color: #ffffff;
	}
	.video-info {
		padding: 1.25rem;
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
		flex-grow: 1;
	}
	.video-title {
		font-size: 0.95rem;
		font-weight: 700;
		color: var(--text-primary);
		line-height: 1.45;
		display: -webkit-box;
		-webkit-line-clamp: 2;
		-webkit-box-orient: vertical;
		overflow: hidden;
	}
	.video-meta {
		display: flex;
		flex-direction: column;
		gap: 0.15rem;
		margin-top: auto;
	}
	.video-author {
		font-size: 0.8rem;
		color: var(--accent);
		font-weight: 600;
	}
	.video-views-date {
		font-size: 0.75rem;
		color: var(--text-secondary);
	}

	/* Related Articles Styles */
	.articles-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 1.5rem;
	}
	.article-card-small {
		position: relative;
		overflow: hidden;
	}
	.article-link-small {
		padding: 1.5rem;
		display: flex;
		flex-direction: column;
		height: 100%;
		text-decoration: none;
	}
	.article-meta-small {
		display: flex;
		justify-content: space-between;
		align-items: center;
		margin-bottom: 0.8rem;
	}
	.article-date-small {
		font-size: 0.75rem;
		color: var(--text-secondary);
	}
	.article-title-small {
		font-size: 1.05rem;
		font-weight: 700;
		color: var(--text-primary);
		margin-bottom: 0.5rem;
		line-height: 1.4;
		display: -webkit-box;
		-webkit-line-clamp: 2;
		-webkit-box-orient: vertical;
		overflow: hidden;
	}
	.article-desc-small {
		font-size: 0.82rem;
		color: var(--text-secondary);
		margin-bottom: 1.25rem;
		display: -webkit-box;
		-webkit-line-clamp: 2;
		-webkit-box-orient: vertical;
		overflow: hidden;
		line-height: 1.5;
	}
	.article-read-link-small {
		font-size: 0.82rem;
		color: var(--accent);
		font-weight: 700;
		margin-top: auto;
		transition: transform 0.2s ease;
	}
	.article-card-small:hover .article-read-link-small {
		transform: translateX(4px);
	}

	/* Responsive Styles */
	@media (max-width: 1024px) {
		.details-grid { grid-template-columns: 1fr; }
		.videos-grid, .articles-grid { grid-template-columns: repeat(2, 1fr); }
	}
	@media (max-width: 768px) {
		.stock-header-panel { flex-direction: column; align-items: stretch; text-align: left; }
		.stock-price-section { align-items: flex-start; text-align: left; }
		.videos-grid, .articles-grid { grid-template-columns: 1fr; }
	}
</style>

# AI-DLC - フェーズ 0: プラットフォーム基盤構築と移行

モノレポ化、テーマの定義、トップページ・共通コンポーネントおよび個別ページの基盤実装を完了しました。

---

## 📋 実績・Todoリスト

- [x] **モノレポ化とリポジトリ再編**
  - [x] リポジトリ名を `investment-platform` に統一
  - [x] `techmoney-web` と `kabumaru-web` の2つのAstroワークスペースを結合し、npm workspacesで管理
- [ ] **デザインシステム、共通UIの実装、および全体UXの確認（ユーザーレビュー待ち）**
  - [ ] **TechMoney** (ダーク / Electric Blue / Glassmorphism)
    - [ ] `techmoney-web/src/styles/global.css` の作成
    - [ ] ヘッダー、フッター、新着記事カードの実装
    - [ ] **全体UX確認（スクロール、ホバー時のインタラクション、動画インポートの見え方）**
  - [ ] **かぶまる** (※フェーズ4へ延期)
- [ ] **個別・動的ページの実装（ユーザーレビュー待ち）**
  - [ ] **TechMoney 個別実装**
    - [ ] AI注目銘柄ランキングの2カラムレイアウト、スコア順位表、銘柄スパークライン表示
    - [ ] 銘柄詳細ページ (`stock/[ticker]`) のNVDA等の個別レンダリング
    - [ ] AIスコアのペンタゴンレーダーチャート（SVG）の描画
    - [ ] 主要財務指標、関連動画・関連記事の自動フィルタリング表示
  - [ ] **かぶまる 個別実装** (※フェーズ4へ延期)
- [ ] **インポートパイプラインの構築（ユーザーレビュー待ち）**
  - [ ] `/Users/mamoru/techmoney` および `kabu_maru` の「+」付きフォルダから自動的にブログへ記事を書き出す Python スクリプト `import_youtube.py` の実装
  - [ ] 年月ディレクトリ（例: `202606/`）に対応するための `os.walk` スキャン拡張
- [ ] **生HTMLのそのまま配信ルーティング対応（ユーザーレビュー待ち）**
  - [ ] `video_summary.html` を Astro の `src/pages/blog/` ディレクトリに直接 `.astro` ファイルとして書き出し、Markdownパーサーを回避して忠実に表示する仕組みへの移行
  - [ ] コレクション（記事一覧）と統合するためのプレースホルダー `.md` の同時生成設計

- [ ] **追加要望タスク (方法・実装方針)**
  - [ ] **既存の動画URL（4月〜6月）の自動マッピングとurl.mdの自動生成**
    - **方法**:
      - `scripts/fetch_youtube_videos.py` またはブラウザ開発者ツールを用いて、TechMoney（`@TechMoney-1billion`）およびかぶまる（`@kabumaru`）の4月〜6月の動画タイトルとURLのJSONリストを取得する。
      - `scripts/map_youtube_urls.py` を実行して、Google Drive上の年月フォルダ配下（`202604`, `202605`, `202606`）で `url.md` がないものに対し、Gemini APIを利用して該当する動画URLを特定して `url.md` を自動配置する（存在しない場合は `none_url.md` を生成する）。
  - [ ] **タイトル編集機能の検討と構築**
    - **方法**:
      - *スプレッドシート管理*: スプレッドシート（またはローカルのCSV/JSONマッピングファイル）に `[ディレクトリ名, カスタムタイトル, カスタム概要]` を定義し、Pythonスクリプト実行時にそれを読み込んでMarkdown/Astroのメタデータを上書きする。
      - *管理画面*: Astro内でローカルからのみアクセス可能な `/admin` 等のルートを作成し、Node.jsのファイルシステムAPIを用いてMarkdownのフロントマターを直接編集するシンプルな管理UIを作成する。
  - [ ] **記事ページからHome（トップ）ページへの遷移改善**
    - **方法**:
      - すべてのカスタムレイアウト記事（`.astro` 形式）に、共通の `<Header />` および `<Footer />` コンポーネントを埋め込み、サイト内の一貫したヘッダーナビゲーションを提供する。
      - 記事の上部やパンくずリストの近くに、トップページへの復帰リンク（「← ホームに戻る」）を配置する。
  - [ ] **記事ページのUI・レイアウトの左側目次（TOC）サイドバー統一**
    - **方法**:
      - `import_youtube.py` の生成ロジックが吐き出すHTML構造を、左側に目次（`toc-sidebar`）、右側に本文（`post-body`）を配したフレックス2カラムレイアウトに統一する。
      - 既存の `AI.astro` や `QCOM.astro` などの目次（現在は上部配置など）を、CSS（`position: sticky;`）を用いたスクロール追従型の左側サイドバー形式に修正する。
  - [ ] **話し手の対話形式（パターンA）とレポート形式（パターンB）の比較検証**
    - **方法**:
      - 任意の1つの記事（例: `google`）を対象に、対話形式の吹き出しUIを用いた「パターンA」と、従来の「パターンB（レポート形式）」の2つのバリエーションを作成する（例: `/blog/google-pattern-a` と `/blog/google-pattern-b`）。
      - それ以外の記事は、一旦すべてレポート形式（パターンB）で自動インポートする。

